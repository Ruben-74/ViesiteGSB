    <nav class="navbar navbar-expand-lg navbar-dark bg-dark nav-fixed">
        <a class="navbar-brand" style="font-size:30px;" href="{{path('visiteurs_index')}}">Gestion des visiteurs</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a href="#"class="btn btn-outline-warning">Recherche</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                   <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toogle" data-toggle="dropdown"  id="accountDropdownLink">
                            <img src="{{app.user.coverImage}}" class="avatar avatar-mini" style="margin-right:20px;" alt="Avatar de {{app.user.FullName}}"><strong>{{app.user.FullName}}</strong>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledBy="accountDropdownLink">
                            <a href="{{path('visiteurs_create')}}" class="dropdown-item">Créer un visiteur</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{path('account_index')}}" class="dropdown-item">Mon compte</a>
                            <a href="{{path('account_profile')}}" class="dropdown-item">Modifier mon Profil</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{path('account_logout')}}" class="dropdown-item">Déconnexion</a>
                        </div>
                   </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{path('account_register')}}"  style="margin-right: 20px;" class="btn btn-outline-primary"> Inscription</a> 
                    </li>
                     <br>
                     <br>
                     <li class="nav-item">
                        <a href="{{path('account_login')}}" class="btn btn-outline-success"> Connexion</a>
                    </li>
                {% endif %}
            </ul>
            <br>
        </div>
    </nav>

    {% for label, messages in app.flashes %}
        <div class="container">
            <div class="alert alert-{{label}}">
                {% for message in messages %}
                    <p>{{ message | raw }}</p>
                {% endfor %}
            </div>
        
        </div>

    {% endfor %}